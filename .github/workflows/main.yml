name: CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Install semgrep
        run: |
          curl -Lo semgrep https://github.com/returntocorp/semgrep/releases/latest/download/semgrep && chmod +x semgrep
          sudo mv semgrep /usr/local/bin/

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install
        
   
      - name: Run semgrep
        run: semgrep ci
